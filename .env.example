# Без слэша в конце. Для прод-сборки: канонический URL и OG ведут на главный блок /#home
# В этом репозитории прод-значение задано в .env.production (npm run build подхватит автоматически).
VITE_SITE_URL=https://example.com
